# install with conda env create -f adsc_3610_env.yaml

name: adsc_3610
channels:
  - conda-forge
  - defaults
dependencies:
  - python
  - ipykernel
  - pandas
  - numpy
  - matplotlib
  - scikit-learn
  - pip
  - flake8
  - black
  - pyspark
  - pymongo
  - pytest
  - pip:
    - otter-grader
